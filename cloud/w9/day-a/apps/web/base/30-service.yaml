apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: gitops-demo
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
